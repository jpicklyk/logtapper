name: USB Ethernet Diagnostics
version: 1.0.0
description: USB-attached Ethernet analysis — enumeration health, interface lifetime, IP configuration auditing, and framework-side flap detection
author: LogTapper
tags: [network, ethernet, usb, kernel, android]
category: network
processors:
  - usb-enumeration-health
  - usb-netdev-lifetime
  - ethernet-config-audit
  - ethernet-interface-flap
