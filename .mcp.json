{
  "mcpServers": {
    "logtapper": {
      "type": "stdio",
      "command": "node",
      "args": ["--experimental-strip-types", "mcp-server/src/index.ts"],
      "description": "LogTapper log analysis tools — query live sessions, sample lines, and get state-tracker events"
    }
  }
}
