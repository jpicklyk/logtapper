type: transformer
id: __pii_anonymizer
name: PII Anonymizer
version: 1.0.0
author: LogTapper
description: Redacts personally identifiable information (emails, IPs, MACs, phone numbers) from log messages. Uses consistent token mapping so the same value always maps to the same token within a session.
tags: [privacy, pii, security]
category: anonymization
builtin: true
builtin_kind: pii_anonymizer