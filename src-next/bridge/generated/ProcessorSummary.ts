// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { TrackerMode } from "./TrackerMode";
import type { VarMeta } from "./VarMeta";

/**
 * IPC-serializable summary (returned by list_processors command).
 */
export type ProcessorSummary = { id: string, name: string, version: string, description: string, tags: Array<string>, builtin: boolean, processorType: string, group: string | null, 
/**
 * Var declarations from the YAML (reporters only; empty for other types).
 */
varsMeta: Array<VarMeta>, license?: string, category?: string, repository?: string, deprecated: boolean, 
/**
 * Whether this processor has a schema contract defined.
 */
hasSchema: boolean, 
/**
 * Marketplace source name (e.g. "official", "my-team"), if installed from a source.
 */
source?: string, 
/**
 * Who installed this processor: `"ui"` or `"agent:<client>"`. Absent for
 * a built-in or a processor installed before this field existed.
 */
installedBy?: string, 
/**
 * Pack ID this processor belongs to, if any.
 */
packId?: string, 
/**
 * State tracker mode. Only set for state_tracker type.
 */
trackerMode?: TrackerMode, 
/**
 * Section names this state tracker targets (bugreport/dumpstate only).
 * Always serialized (as `[]` when empty): the generated TS type declares
 * a required array, and the Solid analyzers surface indexes it directly.
 */
trackerSections: Array<string>, 
/**
 * Whether this state tracker outputs to the timeline. Only set for state_tracker type.
 */
trackerTimeline?: boolean, 
/**
 * Log source types this processor supports (from schema.source_types).
 * Always serialized (as `[]` when empty) — same contract reason as above.
 */
sourceTypes: Array<string>, };
