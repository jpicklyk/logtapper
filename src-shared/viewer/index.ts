/**
 * Public API of the viewer module (principle 9).
 *
 * The absolute-line to rendered-index mapping: a binary search over the sorted
 * line-set array a filtered or section-scoped viewer renders.
 */
export * from './scrollMapping';
