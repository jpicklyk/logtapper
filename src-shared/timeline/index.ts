/**
 * Public API of the timeline module (principle 9).
 *
 * Pure line-axis math for the device-state timeline strip: viewport zoom and
 * pan, tick steps, per-line percentages, plus the compact timestamp/duration
 * formatters the strip labels with.
 */
export * from './timelineUtils';
