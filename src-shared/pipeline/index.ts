/**
 * Public API of the pipeline module (principle 9).
 *
 * The localStorage seed for the default processor chain — the key names are
 * shared, so both the chain and its disabled set round-trip through one place.
 */
export * from './pipelineChainStorage';
