/**
 * Public API of the processors module (principle 9).
 *
 * Presentation helpers for state-tracker variables: value-kind probes, the
 * grouping a dashboard renders (`groupVars`), and the `value — description`
 * split emitted by processor scripts.
 */
export * from './utils';
